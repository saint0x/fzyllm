use core.thread;

fn run() -> i32 {
    log.info("runtime.worker started", log.fields2("component", "runtime.worker", "phase", "loop_start"))
    while true {
        checkpoint()
        runtime.events.emit_health_tick()
        yield()
    }
    return 0
}
